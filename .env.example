# =============================================================================
# ODOO DOCKERIZED DEPLOYMENT - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update the values according to your environment.
# Run: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# ODOO APPLICATION
# -----------------------------------------------------------------------------
# These variables are used by odoo-web container
USER=odoo
HOST=odoo-db

# -----------------------------------------------------------------------------
# POSTGRESQL DATABASE
# -----------------------------------------------------------------------------
# These variables are used by odoo-db container
# Note: The actual password is stored in secrets/pg_password.txt (Docker secret)
POSTGRES_HOST=odoo-db
POSTGRES_DB=postgres
POSTGRES_USER=odoo
POSTGRES_PORT=5432
PGDATA=/var/lib/postgresql/data/pgdata

# -----------------------------------------------------------------------------
# TRAEFIK REVERSE PROXY (Required for traefik.yaml & odoo-traefik.yaml)
# -----------------------------------------------------------------------------

# Your main domain (used for Traefik dashboard: traefik.example.com)
DOMAIN=example.com

# Subdomain where Odoo will be accessible (OPTIONAL)
# If you want Odoo on main domain, change ${SUB_DOMAIN} to ${DOMAIN} in odoo-traefik.yaml
SUB_DOMAIN=odoo.example.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# TRAEFIK DASHBOARD AUTHENTICATION
# -----------------------------------------------------------------------------
# Username for Traefik dashboard login
USERNAME=admin

# Hashed password for Traefik dashboard (bcrypt format)
# Generate using: docker run --rm httpd:alpine htpasswd -n -b -B username password
# Copy only the hash part (after username:) and paste below
# Example: admin:$2y$05$xxx... -> copy $2y$05$xxx...
HASHED_PASSWORD=$2y$05$UHw.pfAQObWpSjqiqYGC0Ox.8ijkd6p2EgiDn2bRF1af8n9g29Uji 